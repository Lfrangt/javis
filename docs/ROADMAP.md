# JAVIS Roadmap

## Phase 0: Desktop Buddy MVP

- Transparent always-on-top desktop buddy.
- Desktop pet stays visually minimal; configuration moves to terminal CUI.
- Realtime voice session.
- Local macOS speech fallback with silent preflight and explicit short audible dogfood.
- Local voice-command fallback that turns a transcript plus metadata-only Mac/UI context into route preview/queued work plus local spoken acknowledgement without starting Realtime, has user-facing `npm run voice -- "..."` and continuous `npm run voice:chat` intake commands, exposes sanitized local history in CUI/API, and is advertised from Realtime health, pet status, work-next, and compact voice handoff when provider quota/auth/network checks block live voice.
- Terminal CUI option `VC` starts the same local no-mic continuous intake loop, while `--print-local-voice-loop` gives a scriptable quickstart for operators and tests; desktop/API fallback defaults stay in the compact pet input, and the API loop opener requires explicit terminal confirmation before it can spawn Terminal. Inside the loop, `/status`, `/app`, `/ui`, `/file`, `/browser`, `/browse`, `/delegate`, `/codex`, `/claude`, `/handoff`, `/next`, `/auto`, `/blockers`, `/unblock`, `/approvals`, and `/history` read compact resident/Mac app/file/browser/work/autonomy/blocker/unblock-preview/approval state without using Realtime. `/app` and `/browser` default to recent ambient metadata for speed, with `--full-app`, `--full-browser`, or `--full` reserved for live Accessibility outline or browser page text. Natural transcripts such as `当前应用有哪些控件`, `浏览器准备好了吗`, `读一下当前网页`, and `当前网页有哪些按钮和输入框` now route directly to read-only current-app UI or browser readiness/page/DOM fast paths before any cloud model call. `/blockers` reads Realtime, approvals, jobs, routes, workflows, attention, and autopilot waiting state without executing, starting workers, opening Terminal, or capturing screen. `/unblock` reads blocker state plus a compact work-next preview without executing work-next, resolving approvals, starting workers, opening Terminal, or capturing screen. `/approvals` reads pending approval/confirmation gates without approving, rejecting, executing, opening Terminal, or capturing screen. `/ui <task>` previews local app/UI workflows, `/file list|search|read ...` inspects allowed files through the file policy, `/file organize|rename|convert ...` previews file workflow plans without moving files, `/browse [intent] <task>` previews browser workflows, `/open <url or search>` previews URL/search actions, and `/delegate`/`/codex`/`/claude` preview scoped worker handoffs with a confirmation gate before execution.
- `/learn` reads the local user-distillation status, reusable habit candidates, privacy boundary, and next review actions in the local no-mic loop without saving memory, exporting skills, granting permissions, executing actions, starting microphone capture, or using Realtime.
- Voice standby prompt pack: the pet status, wake handoff, `/api/voice/standby`, terminal CUI, local `/voice` check, and compact compose placeholder share the same context-ranked next safe utterance plus a few examples, keeping Realtime-blocked standby actionable without adding diagnostics to the desktop pet or opening Terminal.
- Screen sharing and frame analysis.
- Live screen context in active Realtime sessions.
- Private screen mode with renderer-side downscale/blur before API or Realtime delivery.
- Fast Q&A lane.
- Local task router for quick/deep/Codex/Claude lane selection.
- No-model local commands for resident status, Inbox capture/listing, app/URL open, and web search.
- Background deep lane.
- Codex and Claude Code delegation hooks.
- Login-start resident mode.
- Visible resident LaunchAgent status and install helper.
- Electron single-instance guard for resident mode, so repeated launches reuse the existing process and summon the pet instead of starting duplicate API servers or windows.
- Renderer self-recovery for resident mode, so load failures and renderer crashes schedule bounded reloads while keeping the pet parked and minimal, with `/api/renderer/status` and `/api/health` exposing current load/recovery state without renderer URL or token leakage.
- macOS menu bar resident controls.
- Global pet hotkey.
- Global tap-to-summon hotkey that wakes JAVIS and parks it at the notch, opening the compact compose input immediately when Realtime is blocked or unverified.
- Wake handoff status that tells the pet/CUI whether the next invited turn should try Realtime or use local no-mic voice-command fallback, without starting the microphone from the status read.
- Wake-command intake that combines wake plus local no-mic voice-command routing through `npm run wake -- "..."` and `/api/wake/command`.
- Pet startup recovery probe: when provider health is warning/blocked and `OPENAI_API_KEY` is loaded, the pet first runs the no-mic Realtime provider probe and waits for a ready result before calling `getUserMedia`; failed recovery keeps the local voice-command fallback active.
- Global clipboard-to-Inbox capture hotkey.
- Resident notifications for approvals and background task completion.
- Push-to-talk voice mode.
- Runtime/tool activity log in CUI/API, not on the desktop pet.
- Sanitized voice-command history in CUI/API for recovery/debugging without putting logs on the desktop pet.
- Lightweight local voice standby state in `/api/pet/status` so the notch capsule can reflect no-mic fallback readiness without becoming a dashboard.
- Quota-aware local entry: `/api/pet/status` now tells the compact capsule to open the quiet local input path when Realtime is blocked or warning. The renderer uses a temporary 520x56 `compose` strip for that local turn and returns to the 148x40 pet after sending, avoiding repeated microphone startup attempts and preventing surprise Terminal windows. Explicit CUI/CLI terminal-loop starts still use `npm run voice:chat`; API-triggered loop starts require `JAVIS_ALLOW_TERMINAL_VOICE_LOOP=true` plus explicit terminal confirmation before `/api/voice/open-local-loop` can spawn Terminal.
- Natural pet-window control from local voice/no-mic intake: phrases such as `把你挪到左下角` preview a JAVIS-only park target, while `回到刘海并变小` executes only the resident capsule mode/park change and restores the compact Dynamic Island presence without touching other apps or files.
- Natural Inbox capture from local voice/no-mic intake: phrases such as `记一下：...` preview a local Inbox item without writing, and explicit execution saves one cleanup-safe Inbox capture while keeping long-term memory, clipboard text, microphone, and Realtime out of the path.
- Natural keep-awake control from local voice/no-mic intake: phrases such as `今晚别睡，保持后台运行` preview or start the JAVIS-managed launchd/caffeinate job, while `可以睡了` previews stopping it without touching microphone, Realtime, user files, or project files.
- Voice standby primary action endpoint: `POST /api/voice/standby` now previews or runs the current primary voice entry action, opening the compact pet local-input strip when Realtime is blocked while refusing server-side microphone starts or surprise Terminal windows.
- Natural voice-status fast path: local voice/no-mic intake now routes questions such as `实时语音连上了吗`, `为什么现在不能直接说话`, and `麦克风准备好了吗` to `voice_status`, reading `/api/voice/standby` without screen context, microphone startup, Realtime sessions, or Terminal windows; `voice:chat` also exposes the same check as `/voice`.
- Natural perception-status fast path: local voice/no-mic intake now routes questions such as `你现在在看我的屏幕吗`, `最近看到什么窗口`, and `现在监控什么` to `perception_status`, reading consent, screen privacy, cached-screen metadata, ambient app/window/browser metadata, and Accessibility readiness without capturing a new screen frame or returning images/page text; `voice:chat` also exposes the same check as `/see`.
- Natural approval-status fast path: local voice/no-mic intake now routes questions such as `现在有没有需要我确认的审批`, `哪些动作卡在确认`, and `有没有要我点同意` to `approval_status`, reading summarized pending approvals, control mode, and effective risk policy without approving, rejecting, executing actions, starting microphone capture, using Realtime, opening Terminal, capturing screen, or mutating user files; `voice:chat` also exposes the same check as `/approvals`.
- Natural blocker-status fast path: local voice/no-mic intake now routes questions such as `现在有哪些阻塞卡住了`, `卡在哪里`, and `what is blocking you?` to `blocker_status`, reading `/api/blockers` for Realtime, approvals, worker recovery, blocked workflows, routing attention, attention policy, and autopilot waiting state without executing actions, resolving approvals, starting workers, using Realtime, opening Terminal, capturing screen, or mutating user files; `voice:chat` also exposes the same check as `/blockers`.
- Natural unblock-preview fast path: local voice/no-mic intake now routes questions such as `怎么解除这些阻塞`, `下一步能安全准备什么`, and `how can you get unstuck?` to `unblock_preview`, combining blocker state with a compact work-next preview without executing work-next, resolving approvals, starting workers, using Realtime, opening Terminal, capturing screen, or mutating user files; `voice:chat` also exposes the same check as `/unblock`.
- Natural learning/distillation fast path: local voice/no-mic intake now routes questions such as `你从我身上学到了什么`, `你现在怎么蒸馏我的使用习惯`, and `我的习惯有哪些可以变成自动化` to `learning_distillation`, reading only local metadata-derived habit/evolution/reusable-workflow evidence without model calls, Realtime, microphone capture, memory writes, skill exports, permission changes, computer actions, Terminal windows, or screen capture; `voice:chat` also exposes the same check as `/learn`.
- Voice standby work-next action: `actionId=voice:standby_primary` exposes that same current voice entry action through `/api/work/next` for CUI, Realtime tools, and local automation.
- Desktop pet fallback click now previews the voice standby contract and focuses local input instead of executing the terminal-loop primary action, keeping pet behavior aligned with the resident voice entry policy.
- Quiet fallback-ready pet state: warning-level Realtime provider failures with local no-mic fallback available now map to a yellow ambient traffic light instead of red interrupt attention; detailed recovery stays in CUI/API.
- Desktop pet consumes resident presence as a stable 148x40 red/yellow/green traffic-light Dynamic Island capsule instead of showing diagnostic chips.
- Desktop pet uses a lightweight `/api/pet/status` lane for parked state with no raw screen image, model list, learning profile, routing history, workflow logs, or runtime data directory; full doctor/config/briefing/context checks stay in CUI, manual refresh, or slow expanded-panel refresh.

## Phase 1: Reliable Resident Server

- Split the local API service from the Electron buddy UI.
- Add health checks, structured logs, and restart visibility.
- Add readiness diagnostics for setup, permissions, policy, and runtime state.
- Add Mac context visibility for frontmost app/window and clipboard state.
- Add browser context visibility for supported active tabs.
- Add a read-only browser readiness packet for the default current-browser target, bridge recovery steps, and no-window-picker safety through `/api/browser/readiness`, CUI option `BR`, and `npm run browser:ready`.
- Add metadata-only local browser activity summaries from ambient observations into presence, Realtime preflight context, Realtime tools, CUI, and API.
- Add a unified perception consent/status registry for screen, voice, ambient, browser, clipboard, Accessibility/app control, learning, and worker tools through CUI/API/Realtime voice without adding diagnostics to the desktop pet.
- Add read-only active browser page extraction for webpage-aware tasks.
- Persist background task history.
- Persist user-level workflow history with linked jobs and results.
- Add deterministic work briefing over status, blockers, recent work, and next actions, filtering internal eval/doctor evidence out of user-facing Work Next.
- Add deterministic lane contract registry for owner/scope/handoff/risk boundaries before model choice.
- Add a voice-facing routing speed policy for Realtime vs fast model vs background model vs Codex/Claude, with browser/file/app tool-first decisions that list the first structured tools to try before slow model synthesis; the Realtime tool now uses a compact payload and leaves full profiles/samples to API/CUI.
- Add a voice-friendly local capability map over lane contracts, control mode, local execution, collaboration claims/handoff, and work-next so Realtime can decide whether to observe, route, delegate, use browser/file/app tools, or hand work to Codex/Claude without guessing; the Realtime tool keeps this as a compact payload and leaves the full capability map to API/CUI. The CUI also exposes a local control readiness packet that compresses voice, screen, Accessibility, browser, file, Codex, Claude Code, CLI, resident hotkey, perception-consent, and collaboration gates into one takeover-readiness view.
- Add deterministic spoken progress check-ins for background jobs and workflows.
- Add unified work-next execution for one safe next action across the local workbench, including route-level recovery envelopes and autopilot eligibility evidence for trusted failed-job route recovery.
- Add a bounded autonomy loop: current build exposes `/api/autonomy/run`, Realtime `run_autonomy_loop`, CUI `AG`/`AR`, `npm run autonomy`, `npm run autonomy:run`, and local `voice:chat` `/agent <task>` for route -> local learning evidence -> observe -> work-next preview -> optional policy-gated execute -> progress verify -> recovery scan, with preview-only default behavior, an `agencyPlan` primary/fallback/boundary envelope, an `ask_last` `selfRecoveryPlan` for inspect/search/retry/delegate alternatives before user escalation, explicit execute/retry recovery budget, inferred learning as soft local context only, and no direct shell/UI bypass. Natural local voice questions such as `你自己现在能不能继续跑，为什么没自动推进？` and loop `/auto` now read compact autopilot/agency state through a no-model, no-screen, no-mic fast path.
- Add local work sessions for focus goals, notes/events, resume handoffs, automatic evidence capture, local voice/wake fallback session events, spoken check-ins, and end-of-session summaries.
- Add safe local command routing before model calls for basic resident operations.
- Persist explicit local memories for user-approved preferences, project facts, and notes.
- Persist a local inferred learning profile from passive ambient metadata, separate from explicit memory, plus a metadata-only recent-vs-baseline evolution snapshot for explaining what changed over time and reviewable reusable habit candidates that suggest which repeated workflows should become demonstrations, skills, shortcuts, or explicit memories after confirmation; work-next can explicitly preview `learning_habit:<candidate-id>` candidates without making them default actions, saving memory, granting permissions, or executing computer actions.
- Generate reviewable Codex-style skill drafts from local learning, routing, and workflow evidence, with explicit export to user-level `~/.agents/skills`.
- Expose explicit UI demonstration Record & Replay calls in Realtime evidence and the guided live-voice dogfood drill, including safe preview replay, skill drafting, confirmation gates, and no raw screenshot/clipboard storage.
- Codex Record & Replay-inspired workflow distillation: let the user show JAVIS a repetitive app/browser/file workflow once, store only sanitized local step metadata, produce a replay plan plus a Codex/Claude-compatible skill draft, and require explicit confirmation before saving a shortcut, exporting a skill, or replaying any action.
- Record & Replay teaching packet: work-next, API, autopilot, terminal CUI, and Realtime voice can prepare or inspect a local no-recording teaching packet that selects the best current habit candidate, lists the exact voice prompts/endpoints/safety gates for the next user-taught workflow, and leaves actual recording, replay, shortcut save, memory promotion, and skill export behind confirmation.
- Persist confirmed local skill shortcuts so repeated successful workflows can be recalled by phrase and managed from CUI/API/Realtime voice without turning the desktop pet into a dashboard.
- Persist local Inbox captures for clipboard/manual follow-ups and include them in briefing.
- Add read-only Inbox triage for priority sorting and lane suggestions.
- Add explicit process-next Inbox flow for doing one highest-priority capture at a time.
- Route Inbox captures into quick/background/Codex/Claude work from CUI/API or voice tools.
- Copy workflow results back to the clipboard from CUI/API or voice tools.
- Add cancellable background workers with visible logs, structured failure recovery plans, `/api/jobs/recovery` summaries, job-level recovery execution through `/api/jobs/:id/recovery/run`, and Realtime `get_worker_recovery` / `run_worker_recovery` access for attempts, diagnostics, recovery child jobs, and recommended next actions.
- Add a multi-agent lane decision record for every routed task: realtime voice, background, Codex, Claude, owner, lane contract, write scope, parallel group, approval requirement, status, result link, and work-next recovery candidates when the route blocks.
- Add resident status views and spoken check-ins that report active parallel work by lane, owner, blocker, and next action.
- Add a local config validator with terminal CUI diagnostics.
- Add a setup guide that maps blockers to the next safe local setup action.
- Add launchd-managed keep-awake controls for unattended resident/background work: `npm run keepawake`, `npm run keepawake:start`, `npm run keepawake:stop`, and `/api/keep-awake/status` verify `caffeinate` plus `pmset` assertions while allowing display sleep by default.
- Add unified voice standby/fallback status through `/api/voice/standby`, `npm run voice:standby`, and CUI option `VS`, so Realtime quota/auth/provider blockers route cleanly to local no-mic intake without expanding the desktop pet or spawning resident Terminal voice-loop windows.
- Add signed allowlists for local actions.
- Add file tool runtime for list/read/search/write with risk-aware approvals.

## Phase 2: Practical Computer Workflows

- Browser task workflows: current build supports current-page summarize, action extraction, drafting, Q&A, preview-only generic page workflows that create workflow/routing evidence without model calls, queues, or page actions, guarded DOM click/fill/select with an explicit preview safety contract, fixture execute-gate coverage for submit-like DOM targets, confirmed DOM-action approval plumbing, live DOM re-observe immediately before execution, and Level 4 confirmation for submit/send/pay/delete/login-like targets by default, search/compare result-page capture loops with structured candidate links, guarded result-page review, guarded multi-page research synthesis with persisted structured continuation/recovery actions for prepared preview links, unvisited result links, failed pages, and follow-up links, work-next previews for those browser research continuations, and preview-first form-fill drafts that match supplied fields to visible DOM controls before any confirmed fill, run privacy-preserving post-fill verification after confirmed live fills, store only redacted safe field metadata, and surface deterministic routed recovery handoffs for sensitive/manual fields such as passwords without storing those values or submitting forms. `/api/browser/benchmarks` and CUI option `G` now run broader preview-only fixture benchmarks for summarize, extract-actions, DOM action safety contract, submit-like DOM execute gates, confirmed fixture gates, fill-draft redaction and routed sensitive-field recovery, compare/search preview, review-result preview, and research continuation contracts without live browser actions or model calls. Realtime evidence now records sanitized browser read/workflow calls, including safe-preview `run_browser_workflow` evidence for live voice dogfood. `npm run dogfood:browser-live-fill` opens a temporary local form in a supported browser, runs confirmed `fill_draft`, verifies all fields through the live browser bridge, runs one confirmed safe DOM action click after a live DOM re-observe, runs one natural-language `act` workflow click through model planning or local fallback, and checks that none of those paths submitted the form; next, dogfood iterative research from a real live voice session.
- Accessibility tree workflows: current build supports read-only frontmost or requested-app UI tree inspection, lazy Chromium web-hint reads, fast no-window fallback, dry-run UI target planning, guarded Level 3 AX press/value-action plumbing, one-step current/requested-app control, current-state workflow planning, and short multi-step app workflows. `/api/app/benchmarks` and CUI option `U` now run preview-only app workflow checks for deterministic open/close planning, typed-text planning, current-app-control planning, explicit multi-step previews, unsafe delete rejection, and no-history/no-app-launch contracts; next, richer permissioned app-specific workflows.
- File workflows: current build supports list/search/summarize/Q&A, guarded by-type organization plans, batch rename previews, deterministic semantic text-conversion previews for `.txt`/`.md`/`.html`/`.json`/`.csv`/`.tsv`, non-destructive copy-convert previews, redacted generated write plans, explicit `confirm:true` plan apply, Level 3 create/copy/move/rename/write actions, and post-apply verification for written/copied/moved artifacts. `/api/files/benchmarks` and CUI option `F` now run preview-only fixture benchmarks for list/search, organization, rename, semantic conversion redaction, copy-convert, and apply-confirmation gates without user-file mutation, app launches, or model calls; next, richer app-specific file result checks and binary/document converters.
- Calendar, email, notes, and reminders: current build supports a productivity app workflow bridge for Notes, Reminders, Calendar, and Mail with staged action packs, native macOS automation for confirmed Notes/Reminders/Calendar/Mail-draft creation, Realtime tools, `/api/productivity/workflow`, `/api/productivity/action`, `/api/productivity/benchmarks`, CUI option `Y`, and `npm run dogfood:productivity-live`. Preview-only benchmarks cover note capture planning, native note-create preview, reminder creation planning, calendar confirmation gates, email draft planning, missing-recipient gates, and email-send blocking without app launches, model calls, workflow-history writes, or UI/file mutations. The productivity dogfood path now runs a four-app Notes/Reminders/Calendar/Mail draft suite and saves a local safety/evidence archive from the script, resident API, Realtime voice tools, or work-next/autopilot preview-only archive action; Realtime evidence and the guided live-voice drill include the safe-preview `save_productivity_dogfood_archive` proof. Next, run the confirmed live dogfood across all four apps and compare post-action observation evidence against that archive format.
- Obsidian/MCP bridge for notes and knowledge work: current build supports local Obsidian/Markdown vault discovery, read-only Markdown note search with snippets/tags/wikilinks, preview-first note creation/append/daily-note plans, read-only local MCP server discovery for Claude Desktop/Claude Code/Cursor/project JSON configs, preview-only MCP workflow planning that selects candidate servers without starting commands or calling tools, local pending approval requests for selected MCP server/tool pairs, approved stdio `initialize` + `tools/list` schema inspection, Realtime voice `plan_mcp_tool_call` approval planning, separately approved stdio `tools/call` execution with sanitized result storage, confirmed fixture-only write benchmarks, Realtime tools, `/api/knowledge/vaults`, `/api/knowledge/search`, `/api/knowledge/workflow`, `/api/knowledge/benchmarks`, `/api/mcp/servers`, `/api/mcp/workflow`, `/api/mcp/tool-call`, CUI option `K`, CUI option `X`, CUI option `W`, and CUI option `Z`. MCP discovery/planning/approval creation redacts env values and never starts server commands; approval can briefly start a stdio server to list schemas or run exactly one confirmed tool call. Writes still go through file policy and `confirm:true`; next, dogfood a configured real vault/MCP tool through live voice where policy allows and add remote MCP transport adapters.
- Creative app bridge: current build recognizes video editing and music composition tasks, chooses common NLE/DAW apps, records stage action packs for imports, timeline edits, subtitles, MIDI sketches, mix/export previews, executes one guarded action at a time, and performs post-action screen/UI verification with recovery hints. `/api/creative/benchmarks` and CUI option `C` now run preview-only fixture benchmarks for video/music planning, export confirmation gates, missing asset-path gates, and prompt previews without app launches, model calls, workflow-history writes, or file/UI mutations; next, add live app-specific result checks for Final Cut Pro, Resolve, Logic, GarageBand, and Ableton.
- Coding workflows through Codex and Claude Code with clear owner/scope boundaries, progress updates, and parallel routing for independent work. Current build records per-task ownership metadata, exposes `npm run collab`, CUI option `27`, `npm run config -- --print-collaboration-suggestions`, and exportable `npm run collab -- handoff --markdown/--write --agent claude-code` packets for external Claude Code/Codex claim-heartbeat-release-handoff coordination plus suggested non-overlapping scopes, returns `/api/collaboration/handoff` and `/api/collaboration/suggestions`, includes that handoff and suggestions in `/api/capabilities` and Realtime `get_local_capabilities`, returns Realtime `get_collaboration_state` handoff summaries for active owners/conflicts/next actions/suggested scopes, exposes Realtime `plan_collaboration_claim`, `heartbeat_collaboration_claim`, and `release_collaboration_claim` for preview-confirm-heartbeat-release ownership control without starting workers or mutating files, exposes Realtime `delegate_task` so live voice can preview a scoped background/Codex/Claude worker handoff and refuse execution until `execute:true` plus `confirm:true`, and serializes overlapping write scopes instead of launching competing agents against the same files; `npm run eval -- --only=parallel,collaboration,realtime` dogfoods read-only investigations, scoped documentation conflicts, collaboration CLI/CUI/capability status-handoff evidence, markdown handoff export, Realtime collaboration claim control, and Realtime delegate-task confirmation gates.
- Local no-mic worker handoff: current `voice:chat` supports `/delegate`, `/codex`, and `/claude` as preview-first shortcuts over the same `delegate_task` path, including the explicit execution confirmation gate before any worker starts.
- Live worker dogfood is opt-in: `npm run dogfood:workers-live` queues real read-only Codex, Claude, and local CLI jobs, then verifies job logs, attempts, cancel state, and explicit internal `/api/work/progress` recent job links while default progress stays user-facing.
- Overnight autopilot: current build skips manual-only actions such as microphone/live-voice start, records structured decision evidence with candidate counts, skip summaries, and explicit waiting conditions, exposes full evidence to CUI/API and a compact status payload to Realtime voice, advances eligible recovery work including trusted routed failed-job recovery, executes the separate safe `realtime_voice:prepare_preflight_bundle` fallback when live voice is blocked but no-mic dogfood preparation can still progress, avoids repeating a fresh no-mic Realtime preflight archive until its cooldown expires, can save stale no-recording Record & Replay teaching packets from local learning candidates, can save stale preview-only Productivity dogfood archives without launching apps or mutating user data, reports teaching-packet and productivity-archive cooldown state while fresh, and falls back to a cooldown-gated read-only maintenance snapshot when no user-visible action is auto-executable.
- Realtime worker dogfood: `npm run dogfood:realtime-live` keeps a live conversation state, queues Codex/Claude/local read-only workers, records `realtime.progress_injection`, verifies grouped internal worker progress, and checks `spokenSummary` is short enough for voice progress answers.
- Realtime voice evidence monitor: current CUI option `V. Watch Realtime voice evidence` watches the real renderer/WebRTC evidence checklist, including structured status/phase/blocker output, renderer-recorded SDP offer/answer evidence, renderer live/data-channel state, click-to-live/negotiation/live-to-progress latency receipts, current-vs-injected work-progress sequence sync, a guided `/api/realtime/dogfood/drill` checklist, a stable `gapSummary` with the next missing step and prompt, recent sanitized Realtime tool-call metadata, compact work-handoff evidence for `get_work_handoff`, compact work-next preview/run evidence for `get_work_next` and `run_work_next`, delegate-task preview/confirmation evidence for `delegate_task`, approval-review evidence for `get_pending_approvals` and `resolve_approval`, collaboration claim-control evidence for `get_collaboration_state`, `plan_collaboration_claim`, `heartbeat_collaboration_claim`, and `release_collaboration_claim`, compact autopilot-status evidence for `get_autopilot_status`, attention-explanation evidence for `get_attention_explanation`, compact perception-consent evidence for `get_perception_consent`, compact local-capability evidence for `get_local_capabilities`, privacy-safe local-learning evidence for `get_learning_profile`, `get_learning_evolution`, compact `get_learning_distillation`, no-recording Record & Replay teaching-packet evidence for `get_record_replay_teaching_packet`, browser read/workflow evidence for `read_browser_page` and `run_browser_workflow`, shortcut list/candidate/save/forget evidence, Realtime dogfood-session voice tool evidence for inspect/start/mark/end calls, and the `/api/realtime/dogfood` manual runbook. Realtime voice can call `get_realtime_evidence` to explain whether live WebRTC/progress is connected, what is blocked, and the next dogfood step, can call `get_work_handoff` to answer "where are we / what next" from a compact summary instead of the full briefing/collaboration ledger, can call `get_perception_consent` to answer what JAVIS can see/store/control from a compact consent summary instead of the full audit registry, can call `get_local_capabilities` to choose browser/file/app/Codex/Claude/local tools from a compact capability map instead of the full lane/collaboration ledger, can call `get_autopilot_status` to explain why unattended work did or did not continue from a compact decision summary instead of the full preview object, can call `get_work_next` to preview the single next safe workbench action without executing it using a small voice payload instead of the full workbench JSON, can call `delegate_task` to preview a scoped background/Codex/Claude task and stop at the confirmation gate before starting a worker, can call `get_pending_approvals`/`resolve_approval` to review summarized approvals and reject or confirm one exact id through the existing approval queue, can call `plan_collaboration_claim`/`heartbeat_collaboration_claim`/`release_collaboration_claim` to preview-confirm-refresh-release scoped Codex/Claude ownership without starting workers or editing files, can call `get_learning_distillation` to answer what JAVIS has locally inferred, what changed recently, which workflows look reusable, and which confirmation boundaries still apply, can call `get_record_replay_teaching_packet` to prepare or save a local teaching packet without starting recording or replay, and can call `get_realtime_dogfood_session`, `start_realtime_dogfood_session`, `mark_realtime_dogfood_step`, and `end_realtime_dogfood_session` to maintain the same operator drill record without starting microphone capture. CUI option `D. Start Realtime dogfood drill` and `/api/realtime/dogfood/start` explicitly summon the pet and can schedule a short local read-only progress sample after the renderer reports a live voice session. CUI option `P. Copy next Realtime dogfood prompt`, `npm run config -- --print-realtime-dogfood-prompt`, `/api/realtime/dogfood/prompt`, and the dry-run copy API give operators the next manual/spoken prompt without starting microphone capture. CUI option `T. Track Realtime dogfood session`, `/api/realtime/dogfood/session`, and the step mark/end APIs keep a local operator record for real spoken drills while keeping the desktop pet free of diagnostics; active session snapshots now auto-sync current evidence into sticky persisted step progress, so completed evidence remains visible after voice disconnects. `/api/work/next` uses the same no-mic preparation cockpit as `npm run dogfood:realtime-prepare` when the blocker is `needs_live_session`, then prints the final mic-confirmed renderer command and carries a structured dogfood guide plus a workbench `actionPlan` with previewable no-mic preparation and manual live-voice gates; this action is manual-only so unattended autopilot never starts microphone/live voice, and no desktop pet diagnostics are added.
- Realtime dogfood operator brief: CUI option `B`, `npm run config -- --print-realtime-dogfood-brief`, and `/api/realtime/dogfood/brief` provide a one-page readiness summary, gap summary, next prompt, follow-up script, evidence gates, active session state, and start/monitor commands without starting microphone capture.
- Realtime live drill pack: CUI option `O`, `npm run config -- --print-realtime-dogfood-pack`, and `/api/realtime/dogfood/pack` bundle renderer preflight, mic-confirmed start, evidence monitor, next prompt, session tracker, archive save, acceptance check, and explicit safety gates into one read-only operator packet before a real voice run.
- Realtime dogfood archive and acceptance: CUI option `A`, `npm run config -- --save-realtime-dogfood-archive`, `GET/POST /api/realtime/dogfood/archive`, `/api/realtime/dogfood/archives`, and Realtime `save_realtime_dogfood_archive` preserve the current dogfood brief, gap summary, evidence checklist, session tracker, and related audit trail as local JSON without starting microphone capture or storing raw audio. CUI option `E`, `npm run dogfood:realtime-acceptance`, `npm run dogfood:realtime-acceptance -- --save-archive`, `GET/POST /api/realtime/dogfood/acceptance`, and Realtime `get_realtime_dogfood_acceptance` turn the same evidence/archive into grouped pass/gap gates plus an `actionPlan` that separates previewable preparation, local JSON audit steps, and manual live-voice/mic-confirmed steps for live WebRTC voice, passive progress, spoken answer, work/autopilot/attention/perception/capability/collaboration/learning/browser tools, UI demonstration learning, shortcut save/recall/forget, and saved local archive, so a run is not treated as accepted until all gates pass; `npm run config -- --prepare-realtime-shortcut-recall --confirm` and `/api/realtime/dogfood/shortcut-recall` can now prepare the routed shortcut-recall gate without microphone capture, worker startup, or task execution. The Realtime tool returns a compact voice payload and leaves the full archive/evidence packet to CUI/API.
- Realtime dogfood preflight bundle: CUI option `Y`, `npm run config -- --prepare-realtime-dogfood-preflight --confirm`, and `POST /api/realtime/dogfood/preflight-bundle` now combine every safe no-mic preparation step into one operator action: live-run cockpit, operator tracker, shortcut-recall proof, saved local archive, grouped acceptance snapshot, and the final `--execute --confirm-mic` live command. It still starts no microphone, no worker, and no task execution.
- Renderer Realtime dogfood trigger: CUI option `R`, `GET /api/realtime/dogfood/renderer`, and plain `npm run dogfood:realtime-renderer` now provide a read-only preflight for provider readiness, renderer availability, next prompt, blockers, and confirmation gates without starting microphone capture. `npm run dogfood:realtime-prepare` / `--prepare-live` now give a no-mic live-run cockpit that loads the full prompt script, starts or reuses the operator tracker, optionally saves a prep archive, and prints the monitor plus final mic-confirmed command. `npm run dogfood:realtime-renderer -- --execute --confirm-mic` and `POST /api/realtime/dogfood/renderer/start` provide the opt-in, mic-confirmed way to make the renderer start the actual WebRTC session, wait for the live data channel, send dogfood prompts, report renderer-stage events, save a local archive, and print the acceptance pass/gap summary through the script. The renderer listener now survives its own `idle -> connecting -> live/error` state transitions during startup, `--prompt-script` feeds the full dogfood prompt script through preview or live runs, `--require-acceptance` automatically uses that full script, polls acceptance while the live run is active, prints the four live gate states, keeps the session open instead of using the default short smoke auto-stop, prints provider quota/rate-limit blockers from `/api/realtime/evidence` instead of only reporting a generic timeout, returns nonzero unless every dogfood acceptance gate passes, and `--acceptance-only --no-save-archive` prints the current acceptance report without starting microphone capture.
- Verifiable next task: run `npm run dogfood:realtime-prepare` or `GET /api/work/next?actionId=realtime_voice:needs_live_session` first to preview the full prompt script and operator tracker without microphone capture, or explicitly run `POST /api/work/next` with `execute:true` and that `actionId` to prepare the full no-mic preflight bundle before the live run. Then run the guided drill through a real renderer/WebRTC voice session with the renderer trigger and CUI evidence monitor open until the drill marks progress answer, handoff tool call, collaboration claim preview/create/heartbeat/release, autopilot status tool call, attention explanation tool call, perception consent, local capability map, local learning profile/evolution/distillation, browser read/workflow evidence, UI-demonstration Record & Replay, and shortcut list/save/forget as ready. The routed shortcut-recall gate can be prepared ahead of the live run with `npm run config -- --prepare-realtime-shortcut-recall --confirm`; after live voice gaps pass, save a local dogfood archive and confirm `npm run dogfood:realtime-acceptance` reports accepted.

## Phase 3: Natural Collaboration

- Wake-word or push-to-talk mode.
- Better interruption handling.
- Ongoing screen context with privacy zones: current build supports global private/clear screen mode plus local app/window/browser-host/region privacy rules in `/api/screen/privacy`; app/window/browser-host exclusion rules block screen image use in server-side quick/observe/vision/Realtime preflight model context, while enabled region rules are pixel-masked in the resident process before resident-captured or renderer-posted frames are stored, returned through the API, or injected into Realtime. The recommended `sensitive_defaults` preset previews/applies password-manager, account/login, payment/banking, security-window, and notification-strip region rules through API, CUI, and setup-next without granting action permissions. Region masks can now be added from API/CUI using common presets such as menu bar, notch band, top-right notifications, Dock, and sidebars, or a custom CUI percentage rectangle; Realtime voice can read screen privacy state, preview region presets, and apply a named region mask after explicit user intent. Next, add a visual drag-to-mask picker.
- Task memory scoped to the local machine: current build supports explicit local memories plus a controllable local inferred learning profile and local evolution snapshot with pause/resume, prompt inclusion, exclusions, deletion, promotion to memory, routing evidence, reviewable reusable habit candidates, reviewable skill draft generation, explicit UI demonstration records available from API/CUI/Realtime voice that turn sanitized app/browser/accessibility captures into manual-preview playbooks, safe replay plans, confirmation-gated replay runs, confirmation-gated local skill drafts, read-only local skill recall that writes structured `skillRecallPlan` evidence into task routing and queued worker execution, and confirmed local shortcut phrases for repeated successful skill plans with CUI/API/Realtime list/save/forget controls plus short Realtime tool-call evidence; next, dogfood shortcut recall and save/forget from a real live voice session.
- Capture queue scoped to the local machine: current build supports persistent Inbox items, read-only triage with lane/source/priority grouping, voice-ready confirmation policies, explicit process-next, and routing them into work lanes; next, dogfood spoken confirmation from a real live voice session.
- Work sessions scoped to the local machine: current build supports start/resume/status/note/check-in/end plus automatic evidence from Inbox, jobs, workflows, approvals, local voice/wake command events, session-aware work-next continuation of the latest executable voice route, and a voice-ready work handoff over progress/session/collaboration/next actions; `/api/realtime/evidence` now shows whether live voice actually called `get_work_handoff`; next, dogfood the handoff in a real live voice session.
- Long-running work status scoped to the local machine: current build supports deterministic progress check-ins over jobs and workflows plus a work-progress sequence that lets active Realtime voice sessions sync grouped worker updates when job/workflow/routing state changes; `/api/realtime/evidence` reports whether the voice layer has the current sequence or is stale; next, make those live updates richer and more app-specific.
- Presence status scoped to the local machine: current build exposes standby/watch/work/listening state, quiet attention policy, compact pet-state mapping for the traffic-light pet, policy-gated attention notification throttling for approvals/setup/voice errors, operator-only attention history in API/CUI without desktop pet diagnostics, and Realtime `get_attention_explanation` for short spoken attention explanations; next, dogfood the explanation in a real live Realtime session.
- Continue-from-history prompts: current build previews and runs workflow continuations with parent workflow context, related recent workflow records, explicit memories, local skill recall, inferred learning profile/evolution evidence, proactive follow-up suggestions in briefing/work-next, persisted browser research continuation actions, and short spoken handoffs that mention continuation candidates; next, use those handoffs during live Realtime dogfood.

## Phase 4: Trust And Autonomy

- Per-action risk levels.
- User approval queue for sensitive operations.
- Replayable audit log.
- Dry-run mode for local automation.
- Evaluation suite for voice latency, task success, and safety behavior.
